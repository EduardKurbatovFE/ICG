import ScreenGradientContainer from '@/components/common/screenGradientContainer';
import { Text } from 'react-native';

const Main = () => {
  return (
    <ScreenGradientContainer>
      <Text>hello this is the main screen</Text>
    </ScreenGradientContainer>
  );
};

export default Main;
